💀 ═══════════════════════════════════════════════════════════════
💀 GPU PLASMA — mode 13h vibes in a real native window
💀 ═══════════════════════════════════════════════════════════════
💀 classic demoscene plasma at 320x200 — 1/4 res, 4x4 fat pixels
💀 ESC or close window to quit
💀 ═══════════════════════════════════════════════════════════════

plug in gpu
plug in math

yeet W = 320
yeet H = 200
yeet w = gpu.spawn(W, H, ✨genz++ gpu plasma✨)

💀 logic resolution — 1/4 of window, each pixel is a 4x4 block
yeet PX = 4
yeet LW = W / PX
yeet LH = H / PX

yeet t = 0

vibe check w.serve() tho
    sus w.pressed(✨esc✨) tho
        dip
    bet

    yeet y = 0
    vibe check y < LH tho
        yeet x = 0
        vibe check x < LW tho
            💀 classic plasma: sum of sines
            yeet v1 = math.vibes(x * 0.08 + t * 0.07)
            yeet v2 = math.vibes(y * 0.1 + t * 0.05)
            yeet v3 = math.vibes((x + y) * 0.06 + t * 0.04)
            yeet v4 = math.vibes(math.glow(x * x + y * y) * 0.05 + t * 0.03)

            yeet val = (v1 + v2 + v3 + v4) * 0.25

            💀 map to RGB — three shifted sine waves for color cycling
            yeet r = math.humbled(math.ate(math.vibes(val * 3.14159 + t * 0.02)) * 255)
            yeet g = math.humbled(math.ate(math.vibes(val * 3.14159 + 2.094 + t * 0.03)) * 255)
            yeet b = math.humbled(math.ate(math.vibes(val * 3.14159 + 4.188 + t * 0.015)) * 255)

            w.drip(x * PX, y * PX, PX, PX, r, g, b)
            yeet x = x + 1
        bet
        yeet y = y + 1
    bet

    💀 title overlay
    w.stan(✨PLASMA✨, 10, 10, 255, 255, 255, 2)
    w.stan(✨ESC=QUIT✨, 10, H - 20, 120, 120, 140)

    w.flaunt()
    yeet t = t + 1
bet

w.unalive()
spill ✨✨
spill ✨💀 plasma complete. the demoscene lives in genz++ now.✨
